linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - gosimple
    - ineffassign
    - revive

linters-settings:
  revive:
    rules:
      - name: exported
        arguments: [checkPrivateReceivers]

run:
  go: "1.21"
